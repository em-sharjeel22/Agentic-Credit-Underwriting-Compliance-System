# # ============================================
# # CRITICAL PINS — do not casually upgrade these.
# # These exact versions resolve real numpy/pandas/torch
# # ABI conflicts hit during development. Upgrading any of
# # them individually can reintroduce those crashes.
# # ============================================
# numpy==1.26.4
# pandas==2.0.3
# pyarrow==12.0.1
# scipy==1.11.4
# pyspark==3.5.1
# shap==0.44.0
# transformers==4.38.0
# sentence-transformers==2.4.0

# # Torch must be installed SEPARATELY from the CPU-only index
# # (a plain `pip install torch` can pull an incompatible/CUDA
# # build). Run this one command by itself:
# #   pip install torch==2.2.2 --index-url https://download.pytorch.org/whl/cpu

# # ── ML / Training ──
# xgboost
# scikit-learn
# optuna
# mlflow
# joblib
# matplotlib
# seaborn

# # ── RAG / Agents ──
# faiss-cpu
# huggingface_hub
# langgraph
# python-dotenv

# # ── Data source ──
# ucimlrepo

# # ── Deployment ──
# streamlit

# ============================================
# DEPLOYMENT requirements — used by Streamlit Cloud.
# For local ML training/EDA work, use requirements-training.txt
# instead (it has the heavier PySpark/torch/mlflow stack).
#
# Intentionally UNPINNED: Streamlit Cloud's Python version moves
# forward over time, and old pinned versions of numpy/pandas often
# have no pre-built wheel for it (they'd have to compile from
# source, which fails in that environment). Leaving these open lets
# the installer pick versions that are actually compatible with
# whatever Python Streamlit Cloud is currently running.
# ============================================
streamlit
pandas
numpy
xgboost
joblib
faiss-cpu
sentence-transformers
huggingface_hub
matplotlib
python-dotenv
torchvision
